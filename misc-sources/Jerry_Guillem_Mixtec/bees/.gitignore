# Ignore all AILLA source files in this folder (raw audio, ELAN/TextGrid,
# original text export, Numbers file, and the finished/superseded review
# CSV) -- keep only the in-progress review CSV under version control.
*

# Un-ignore this file specifically
!myuc1042_draft_ipa_review-in_progress.csv

# Un-ignore this .gitignore itself, so it's tracked too
!.gitignore
