For all file pairs (textgrid + wav) in the first level folder ‘with-txtgrd’ a praat script should be run which pull up each and creates a .txt file from them

 > Then the contents should be moved to ‘w-txtgrd-txt’